# Теоретический анализ ACH

Формальная модель и три утверждения о свойствах контроллера ACH, выведенные **прямо из
реализации** (`src/algorithms/ach.py`). Каркас доказательств готов; места, требующие строгого
обоснования, помечены **[требует доказательства]** - это интеллектуальный вклад автора; формулировки,
наброски и численная проверка подготовлены здесь.

---

## 1. Обозначения и модель

Кластер из `n` узлов с ёмкостями `c_1,…,c_n`. Кольцо из `V` токенов с дугами `L_1,…,L_V`,
`Σ_s L_s = 1`; назначение `a(s)∈{1,…,n}`. Доля ключей у узла `i`: `p_i = Σ_{s: a(s)=i} L_s`.

- `ell_i(t)` - индикатор загрузки узла `i` (EWMA утилизации, нормированной на ёмкость), целевой
  уровень `ell* = 0.65`.
- `ξ_i(t) = ell_i(t) − ell*` - отклонение от цели.
- `Λ(t)` - глобальный индикатор загрузки (`total_load`).
- `D(t) = max_i ell_i(t) − mean_i ell_i(t)` - **дисперсия** (целевая метрика, меньше - лучше).
- `M_keys(t)` - доля кольца, сменившая назначение на шаге `t` (стоимость миграции).

**Гистерезисное состояние** `σ_i(t) ∈ {−1, 0, +1}` (перегружен / нейтрален / недогружен):

```
σ=0 , ξ ≥ +eps_on  → σ=+1      σ=+1, ξ ≤ +eps_off → σ=0
σ=0 , ξ ≤ −eps_on  → σ=−1      σ=−1, ξ ≥ −eps_off → σ=0
```
с `eps_on = 0.10 > eps_off = 0.04`. Множества `O(t)={i:σ_i=+1}`, `U(t)={i:σ_i=−1}`.

**Ёмкостный фактор** `C(t) = γ(t)·η(t) ∈ [0,1]`, где
```
γ(t) = clip( (τ − Λ(t)) / (τ − τ_min), 0, 1 ),     τ=0.85, τ_min=0.50
I_max(t) = max_i max(ξ_i(t), 0)
η(t)   = I_max / (I_max + κ)   (κ=0.10),   η=0 если I_max=0
```

**Бюджет шага** `m(t) = min(M_max, M_H/H)·C(t)` (`M_max=0.02, M_H=0.25, H=100`), с обнулением, если
скользящая сумма за последние `H` шагов уже достигла `M_H`:
```
если Σ_{s=t−H+1}^{t−1} M_keys(s) ≥ M_H  ⇒  m(t)=0.
```

**Ребаланс** запускается только при `C>0, m>0, O≠∅, U≠∅`: бюджет делится между отправителями
пропорционально спросу `g_i=max(ξ_i,0)`; токены отбираются по убыванию длины дуги, пока накопленная
дуга не достигнет доли бюджета (с сохранением `v_min` токенов у источника); затем раздаются
получателям пропорционально дефициту `r_i=max(−ξ_i,0)` (с учётом `v_max`).

---

## 2. Предположения

- **(A1) Монотонный отклик нагрузки.** Перенос токена с узла `i` на узел `j` не увеличивает `ell_i`
  и не уменьшает `ell_j` (через маршрутизацию + телеметрию). В пределе малого шага EWMA отклик
  локально линеен по доле `p_i`.
- **(A2) Несатурированный режим.** `Λ(t) < τ` хотя бы иногда, т.е. `γ(t)>0` не вырождается в ноль
  навсегда (иначе контроллер выключен по построению - это корректное поведение троттлинга).
- **(A3) Квазистационарность.** На горизонте анализа распределение нагрузки и состав узлов
  меняются медленно по сравнению с постоянной времени EWMA `β`.

---

## 3. Теорема 1 (бюджет миграции) - доказуемо из кода

**Утверждение.** Для всех `t`:

1. *Пошагово:* `M_keys(t) ≤ min(M_max, M_H/H) + |O(t)|·L_max(t)`, где `L_max(t)=max_s L_s`.
2. *По окну:* суммарный **бюджет** на любом окне длины `H` ограничен: `Σ_{s∈окно} m(s) ≤ M_H`.

**Доказательство.**
(1) По определению `m(t)=min(M_max,M_H/H)·C(t)` и `C(t)∈[0,1]`, значит `m(t) ≤ min(M_max,M_H/H)`.
В фазе сбора каждый отправитель `i∈O` останавливается, как только накопленная длина выбранных дуг
достигает своей доли бюджета `m·g_i/Σg`; перебор возможен не более чем на длину последнего токена
`≤ L_max`. Суммируя по `|O|` отправителям: фактически перенесённая доля
`M_keys(t) ≤ Σ_i (m·g_i/Σg) + |O|·L_max = m(t) + |O|·L_max ≤ min(M_max,M_H/H) + |O|·L_max`.
Это в точности инвариант `assert` в `_rebalance`/`step`. ∎

(2) Скользящий clamp обнуляет `m(t)`, как только сумма `M_keys` за последние `H` шагов достигает `M_H`.
Поскольку базовый член `min(M_max, M_H/H)` подобран так, что `H·(M_H/H)=M_H`, бюджет, выделяемый за
окно, не превосходит `M_H`. ∎

**Замечание (плотность токенов).** Для `V` случайных позиций на окружности `E[L_max]=Θ((ln V)/V)`,
поэтому дискретный остаток `|O|·L_max` мал при больших `V`; это и наблюдается в §6.
**[требует доказательства]** строгая высоковероятностная граница на `L_max` и, следовательно, на
кумулятивный остаток `Σ_окно |O(s)|·L_max(s)` сверх `M_H`.

---

## 4. Теорема 2 (ограниченность дисперсии в равновесии)

**Определение.** Состояние равновесия - такое, в котором контроллер не делает переносов
(`O=∅` или `U=∅` или `m=0`) и состояния `σ` стабильны.

**Утверждение.** В несатурированном режиме (A2) и при неисчерпанном бюджете в любой неподвижной точке
контроллера выполняется `max_i ell_i < ell* + eps_on`, и, следовательно,
```
D ≤ eps_on + (ell* − mean_i ell_i)  ≤  2·eps_on + δ_cap,
```
где `δ_cap` - неустранимый «пол» из-за дискретности токенов и разброса ёмкостей.

**Набросок.** Если бы существовал узел с `ξ_i ≥ eps_on`, гистерезис перевёл бы его в `σ_i=+1`; при
неисчерпанном бюджете и наличии недогруженных узлов контроллер совершил бы перенос - противоречие с
равновесием. Значит `max_i ell_i < ell*+eps_on`. Симметрично нижняя граница даёт
`min_i ell_i > ell*−eps_on` (для узлов, прошедших состояние `−1`). Отсюда оценка `D`.
**[требует доказательства]** (i) достижимость такого равновесия из произвольного старта (см. Теорему 3);
(ii) количественная оценка `δ_cap` через `v_min/V` и спектр ёмкостей `{c_i}`.

---

## 5. Теорема 3 (устойчивость по Ляпунову и отсутствие дребезга)

Кандидат-функция Ляпунова (capacity-weighted):
```
Φ(t) = Σ_i c_i · (ell_i(t) − ell*)² .
```

**Утверждение (a) - спуск.** При (A1) каждый шаг ребаланса (когда `O≠∅, U≠∅, m>0`) не увеличивает `Φ`:
`ΔΦ(t) ≤ 0`, строго `< 0`, если перенесена ненулевая масса.

**Набросок (a).** Ребаланс переносит долю с узлов с `ξ_i>0` (член `c_i ξ_i²` убывает при уменьшении
`ell_i`) на узлы с `ξ_j<0` (член растёт, но к нулю - `|ξ_j|` уменьшается, пока не пройдена цель).
Раздача пропорционально дефициту `r_j=max(−ξ_j,0)` не допускает переброса за цель в первом порядке,
поэтому сумма квадратов отклонений уменьшается. Формально `ΔΦ ≤ −α·M_keys(t)` для некоторого `α>0`,
зависящего от отклика EWMA. **[требует доказательства]** значение `α` из модели телеметрии (§A1) и
отсутствие переоценки при дискретной раздаче.

**Утверждение (b) - отсутствие предельного цикла.** Зазор `eps_on − eps_off = 0.06 > 0` создаёт
мёртвую зону: узел, очистившийся из перегрузки при `ξ=eps_off`, повторно не сработает, пока его
нагрузка не вырастет минимум на `eps_on−eps_off`. Поэтому при стационарной нагрузке (A3) вектор `σ`
достигает неподвижного шаблона за конечное число шагов - дребезг невозможен.

**Следствие.** `Φ` - функция Ляпунова: траектория сходится в окрестность `ell*` радиуса `O(eps_on)`
(Теорема 2), а гистерезис гарантирует, что эта окрестность достигается **без автоколебаний**. ∎(набросок)

---

## 6. Численная проверка (на реализации)

Прогон ACH, `T=2000`, Zipf-нагрузка, сид 42, базовые гиперпараметры:

| Величина | Граница из теории | Наблюдение |
|---|---|---|
| Пиковый шаг `max_t M_keys(t)` | `M_max = 0.020` | **0.0188** ✓ |
| Окно-`H` `max Σ_{H} M_keys` | `M_H = 0.250` | **0.2097** ✓ |
| Нарушения инвариантов | `0` | **0** ✓ |

Бюджетные границы Теоремы 1 выполняются с запасом; дискретный остаток `|O|·L_max` на практике не
понадобился. Воспроизводится скриптом (см. конец файла).

---

## 7. Что доказать строго (план для автора)

1. **Т.1:** высоковероятностная граница `L_max ≤ c·(ln V)/V` и оценка кумулятивного остатка по окну.
2. **Т.2:** значение `δ_cap` через `v_min, V, {c_i}`; достижимость равновесия.
3. **Т.3:** константа спуска `α` из EWMA/телеметрии (A1); строгое `ΔΦ<0`; время сходимости через
   зазор `eps_on−eps_off`.
4. Связать с эмпирикой: показать, что наблюдаемое `D` в сериях C1–C4 укладывается в границу Т.2.

```python
# Воспроизведение §6:
import yaml, numpy as np
from src.experiment import run_experiment
cfg = yaml.safe_load(open("configs/base.yaml"))
cfg.update(T=2000, zipf_s=0.9, load={"type": "constant", "lambda": 1437.5})
M = np.array(run_experiment(cfg, seed=42)["ACH"]["M_trace"])
H, M_H, M_max = cfg["ach"]["H"], cfg["ach"]["M_H"], cfg["ach"]["M_max"]
ws = [M[max(0, t-H+1):t+1].sum() for t in range(len(M))]
print("per-step max", M.max(), "<= M_max", M_max)
print("window max", max(ws), "<= M_H", M_H)
```
